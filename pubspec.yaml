name: jarvis_assistant
description: AI Assistant with voice output
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: 1.2.2
  flutter_tts: 4.0.2
  shared_preferences: 2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
